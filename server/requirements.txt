mflux>=0.19.1
fastapi>=0.141.1
uvicorn[standard]>=0.52.4
Pillow>=12.3.0
python-multipart>=0.0.32

# For vlm_worker.py, the detection sidecar in this directory. It is a separate
# process the server never imports, but it ships and installs with the server, so its
# one dependency is declared here rather than relied on transitively -- huggingface_hub
# pulls requests in via mflux today, and that is not a contract to build on (the same
# reasoning requirements-dev.txt applies to httpx).
requests>=2.34.2
